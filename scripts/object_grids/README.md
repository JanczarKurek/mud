# Object grids

These files serve as generators of basic bitmaps
for drawing various typical objects like walls
and character sprites. They are meant to encode
proportions to enable experimenting with different
perspectives etc.
